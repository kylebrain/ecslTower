using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//deprecated
public class SelectScript : MonoBehaviour {

    /*
    private void Start()
    {
        ListenWithButton();
    }

    // handle listening to the disconnect function as well as mode select function
    private void ListenWithButton()
    {
        if(Lobby.instance != null)
        {
            Lobby.instance.backButton.onClick.RemoveAllListeners();
            Lobby.instance.backButton.onClick.AddListener(ReturnToModeSelect);
        }
        
    }

    private void OnEnable()
    {
        ListenWithButton();
    }

    void ReturnToModeSelect()
    {
        SceneLoader.LoadScene("ModeSelect");
    } */
}
